/**
* CT30A3370 Käyttöjärjestelmät ja systeemiohjelmointi
* Systeemiohjelmointi-osan harjoitustyö
* 
* Tekijät:
* Petri Rämö, opiskelijanumero: 0438578
* Ilari Sahi, opiskelijanumero: 0438594
*
* 7.1.2017
*/

To run our shell you must compile it with make command and then use command ./simpleshell
Commands that we tested in our shell:
cd some_directory
cd ..
cd
ls -l -A
ls -l -a -S > output.txt
cat output.txt
less output.txt (you can get out of this by pressing q button)
uname -a
wc
md5sum < output.txt
md5sum < output.txt > checksum.txt
cat -b output.txt | wc -l
ls -al | tr a-z A-Z
rm checksum.txt
ps
gedit
gedit &
sleep 10
exit
